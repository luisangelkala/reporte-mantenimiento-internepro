# APK Android

Abrir la carpeta `android/` con Android Studio, permitir la sincronización de Gradle y compilar `app` en modo `debug` para QA. La clave técnica se configura en la tablet durante la prueba; no se incluye en el repositorio.
